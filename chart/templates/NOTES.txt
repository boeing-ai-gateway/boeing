Thank you for installing {{ .Chart.Name }}.

Your release is named {{ .Release.Name }}.

To learn more about the release, try:

  $ helm status {{ .Release.Name }}
  $ helm get all {{ .Release.Name }}

{{ if eq .Values.config.BOEING_SERVER_ENABLE_AUTHENTICATION true }}
You can retrieve the bootstrap token by running:
  $ kubectl get secret -n {{ .Release.Namespace }} {{ include "boeing.config.secretName" . }} -ojson | jq -r .data.BOEING_BOOTSTRAP_TOKEN | base64 -d; echo
{{ end }}