{{- if or .Values.serviceAccount.create (eq .Values.config.BOEING_SERVER_MCPRUNTIME_BACKEND "kubernetes") }}
apiVersion: v1
kind: ServiceAccount
metadata:
  name: {{ include "boeing.serviceAccountName" . }}
  {{- with .Values.serviceAccount.annotations }}
  annotations:
    {{- toYaml . | nindent 4 }}
  {{- end }}
  labels:
    {{- include "boeing.labels" . | nindent 4 }}
{{- end }}
